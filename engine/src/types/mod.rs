//! Type definitions used throughout the Rpress framework.

/// Core type definitions: request/response types, status codes, handler and middleware
/// type aliases, and the `handler!` macro.
pub mod definitions;
